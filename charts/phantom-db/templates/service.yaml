apiVersion: v1
kind: Service
metadata:
  name: {{ include "vandal-db.fullname" . }}
  labels:
    {{- include "vandal-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vandal-db.selectorLabels" . | nindent 4 }}
