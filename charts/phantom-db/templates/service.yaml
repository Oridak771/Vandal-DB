apiVersion: v1
kind: Service
metadata:
  name: {{ include "phantom-db.fullname" . }}
  labels:
    {{- include "phantom-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phantom-db.selectorLabels" . | nindent 4 }}
